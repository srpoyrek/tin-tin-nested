#include <stdio.h>
#include "tt_types.h"

int main(void)
{
    // tensor_t w = tin_alloc(32);      /* weights placeholder */
    // tensor_t a = tin_alloc(32);      /* activation placeholder */

    // for (size_t i = 0; i < 32; ++i)  /* deterministic pattern */
    //     w.data[i] = (int8_t)(i - 16);

    // tin_print("Weights", &w);
    // tin_print("Acts   ", &a);

    // tin_free(&w);
    // tin_free(&a);
    return 0;
}
